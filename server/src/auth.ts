import{Request,Response,NextFunction}from'express';import jwt from'jsonwebtoken';import{User}from'./models.js';const secret=()=>process.env.JWT_SECRET||'development-secret-change-me';export const sign=(id:string)=>jwt.sign({id},secret(),{expiresIn:'7d'});export interface AuthRequest extends Request{user?:any}export async function auth(req:AuthRequest,res:Response,next:NextFunction){try{const token=req.headers.authorization?.replace('Bearer ','');if(!token)return res.status(401).json({message:'Authentication required'});const{id}=jwt.verify(token,secret())as{id:string};req.user=await User.findById(id).select('-password');if(!req.user)return res.status(401).json({message:'Account not found'});next()}catch{return res.status(401).json({message:'Invalid or expired session'})}}export const roles=(...allowed:string[])=>(req:AuthRequest,res:Response,next:NextFunction)=>allowed.includes(req.user.role)?next():res.status(403).json({message:'This action is not permitted for your role'});
