import'dotenv/config';import mongoose from'mongoose';import bcrypt from'bcryptjs';import{Donation,User}from'./models.js';const accounts=[['Demo Donor','donor@foodbridge.demo','donor','Green Spoon Kitchen'],['Hope Foundation','ngo@foodbridge.demo','ngo','Hope Foundation'],['Asha Rider','rider@foodbridge.demo','delivery','FoodBridge Riders'],['FoodBridge Admin','admin@foodbridge.demo','admin','FoodBridge']];async function run(){await mongoose.connect(process.env.MONGODB_URI||'mongodb://127.0.0.1:27017/foodbridge');await Donation.deleteMany({});await User.deleteMany({email:{$in:accounts.map(a=>a[1])}});const users:any={};for(const[n,e,r,o]of accounts){users[r]=await User.create({name:n,email:e,role:r,organization:o,password:await bcrypt.hash('Demo@123',12)})}const t=new Date();const add=(h:number)=>new Date(t.getTime()+h*36e5);await Donation.create([{title:'Fresh vegetable biryani',foodType:'Cooked meal',quantity:'8 insulated trays',meals:55,preparedAt:t,expiresAt:add(3),address:'MG Road, Bengaluru',location:{type:'Point',coordinates:[77.5946,12.9716]},notes:'Vegetarian; packed safely at 6 PM.',donor:users.donor._id},{title:'Assorted bakery boxes',foodType:'Bakery items',quantity:'12 sealed boxes',meals:35,preparedAt:t,expiresAt:add(5),address:'Indiranagar, Bengaluru',location:{type:'Point',coordinates:[77.6412,12.9784]},donor:users.donor._id},{title:'Wholesome lunch packs',foodType:'Cooked meal',quantity:'30 packed meals',meals:30,preparedAt:t,expiresAt:add(2),address:'Koramangala, Bengaluru',location:{type:'Point',coordinates:[77.6245,12.9352]},donor:users.donor._id,ngo:users.ngo._id,status:'claimed'}]);console.log('Seeded demo accounts and listings. Password: Demo@123');await mongoose.disconnect()}run().catch(e=>{console.error(e);process.exit(1)});
