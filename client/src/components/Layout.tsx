import{Link,NavLink,useNavigate}from'react-router-dom';import{useAuth}from'../auth';import type{LucideIcon}from'lucide-react';import{HeartHandshake,LayoutDashboard,PackagePlus,Package,UserCircle,LogOut}from'lucide-react';export default function Layout({children}:{children:React.ReactNode}){const{user,logout}=useAuth();const nav=useNavigate();const links:[string,LucideIcon,string][]=[['/dashboard',LayoutDashboard,'Overview'],['/donations',Package,'Food listings'],...(user?.role==='donor'?[['/donate',PackagePlus,'Post surplus'] as [string,LucideIcon,string]]:[]),['/profile',UserCircle,'Profile']];return <div className="min-h-screen"><header className="sticky top-0 z-20 border-b bg-white/90 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-3"><Link to="/dashboard" className="flex items-center gap-2 text-xl font-bold text-leaf"><span className="grid h-9 w-9 place-items-center rounded-xl bg-leaf text-white"><HeartHandshake size={21}/></span>FoodBridge</Link><div className="flex items-center gap-3 text-sm"><span className="hidden sm:block">Hi, <b>{user?.name}</b></span><button onClick={()=>{logout();nav('/')}} className="btn-outline !px-3 !py-2"><LogOut size={17}/><span className="hidden sm:inline">Sign out</span></button></div></div></header><div className="mx-auto flex max-w-7xl"><aside className="hidden w-56 shrink-0 border-r bg-white p-3 md:block">{links.map(([to,Icon,label])=><NavLink key={to} to={to} className={({isActive})=>`my-1 flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold ${isActive?'bg-mint text-leaf':'text-slate-600 hover:bg-slate-50'}`}><Icon size={18}/>{label}</NavLink>)}</aside><main className="min-w-0 flex-1 p-4 md:p-8">{children}</main></div></div>}
